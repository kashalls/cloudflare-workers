{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "build": "lerna run build --stream --parallel",
    "test": "npx lerna run test",
    "release": "lerna publish -y"
  }
}
