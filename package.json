{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "publish": "npx lerna run publish"
  }
}
